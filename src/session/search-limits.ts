// 1688's offer search currently exposes fixed 60-item remote pages. Keep the
// transport boundary separate from CollectionUnit emission limits.
export const SEARCH_REMOTE_PAGE_SIZE = 60;
export const SEARCH_REMOTE_PAGE_LIMIT = 20;
